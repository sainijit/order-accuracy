# Unified Order Accuracy Service - Environment Configuration

# ====================
# Service Mode
# ====================
# single: Single worker with FastAPI (for video upload/single orders)
# parallel: Multiple workers for concurrent RTSP streams
SERVICE_MODE=single

# ====================
# Worker Configuration (parallel mode only)
# ====================
WORKERS=1
SCALING_MODE=fixed  # fixed or auto

# ====================
# VLM Backend
# ====================
# embedded: Run VLM model in-container (GPU required)
# ovms: Use OVMS service (recommended for production)
VLM_BACKEND=ovms

# ====================
# Quick Start Examples
# ====================

# Example 1: Single worker with OVMS (default)
# SERVICE_MODE=single
# VLM_BACKEND=ovms
# docker compose --profile ovms -f docker-compose.unified.yaml up -d

# Example 2: Parallel mode with 2 workers
# SERVICE_MODE=parallel
# WORKERS=2
# VLM_BACKEND=ovms
# docker compose --profile ovms -f docker-compose.unified.yaml up -d

# Example 3: Parallel mode with autoscaling
# SERVICE_MODE=parallel
# WORKERS=2
# SCALING_MODE=auto
# VLM_BACKEND=ovms
# docker compose --profile ovms -f docker-compose.unified.yaml up -d
